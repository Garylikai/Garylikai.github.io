name: Check website

on:
  pull_request:
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  build-and-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true
          cache-version: 1
          
      - name: Report build versions
        run: |
          ruby --version
          bundle exec jekyll --version
          bundle exec ruby -rliquid -e 'puts "Liquid #{Liquid::VERSION}"'
          
      - name: Build Jekyll site
        run: bundle exec jekyll build --trace

      - name: Check generated internal links
        run: bundle exec htmlproofer ./_site --disable-external
